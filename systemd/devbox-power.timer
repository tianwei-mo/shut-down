[Unit]
Description=Check the devbox shutdown deadline every minute
After=devbox-power-init.service

[Timer]
OnCalendar=*-*-* *:*:00
AccuracySec=1s
Persistent=true
Unit=devbox-power.service

[Install]
WantedBy=timers.target
